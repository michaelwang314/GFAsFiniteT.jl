- check memory allocations, type instabilities, and perform general optimizations
- reproduce structures from PRL
- add initialize for lattice positions
- document code and add comments
- look into Makie for visualizations
    - translate test script into working module
- allow 2D simulations
- add StaticHarmonicTrap and MovingHarmonicTrap (uses function for position)
- check if converting start_id to Vector instead of Array{, 3} is better
- Change BondList to FixedBondList and add a DynamicBondList.  
    - change compute_force! do deal with interaction that use DynamicBondList
    - Restructure how forces are computed so that there's no need to type many repetative functions for each new interaction
- fixed bind_closest to exclude bonds between particles on save body